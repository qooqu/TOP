import initSlider from "./initSlider";

let state = {
    imgsIn: [1, 2, 3, 4, 5],
    imgs: [],
    indexCentered: null,
    imgWidth: 500,
    imgPadding: 20,
    dots: [],
};

export default state;

initSlider();
